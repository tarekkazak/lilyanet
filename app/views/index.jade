doctype html
html
    head
        meta(charset="UTF-8")
        title
        script(src="https://www.google.com/jsapi")
        script.
            var container = (function(word, socketServer, isLocalResource){
                return {
                    letters : word.split(''),
                    socketServer : socketServer,
                    isLocalResource : isLocalResource === 'true'
                };
            })("#{word}", "#{socketServer}", "#{isLocalResource}");
    body
        div(id="container") != content    
        script(src="/js/" + "#{source}")
