doctype html
html
    head
        meta(charset="UTF-8")
        title
        link(rel='stylesheet', href='/stylesheets/main.css')
        script.
            var container = (function(socketServer){
                return {
                    socketServer : socketServer,
                };
            })("#{socketServer}");
    body
        div(class="container-fluid")
            div(class="row")
                div(id="word-container", class="col-md-12") 
            div(class="row")
                div(id="image-container", class="col-md-12") 
            div(class="row")
                div(id="word-selector-container", class="col-md-12") 

        script(src="/js/" + "#{source}")
